import React from 'react';
import sidebarIcon from '../../static/sidebar.png';

const CadreSideCard = () => (
  <div className="cadre-side-card">
    <img
      src={sidebarIcon}
      alt="sidebar icon"
      className="cadre-side-card-icon"
    />
  </div>
);

export default CadreSideCard;
