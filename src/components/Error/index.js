import ErrorPage from './ErrorPage';
import ErrorBoundary from './ErrorBoundary';
import ErrorMessage from './ErrorMessage';

const Error = {
  ErrorPage, ErrorBoundary, ErrorMessage,
};

export default Error;
